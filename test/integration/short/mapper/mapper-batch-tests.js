'use strict';

const mapperTestHelper = require('./mapper-test-helper');


describe('Mapper', function () {

  mapperTestHelper.setupOnce(this);

  describe('#batch()', () => {

    it('should execute a batch containing multiple tables from the same doc', () => {

    });
  });
});