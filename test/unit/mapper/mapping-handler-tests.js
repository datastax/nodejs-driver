'use strict';

describe('MappingHandler', () => {
  describe('#getSelectExecutor()', () => {
    it('should convert property names to column names');

    it('should generate executor once and cache it');

    it('should obtain the parameters each time');
  });

  describe('#getUpdateExecutor()', () => {
    it('should support relational operators for conditional updates');
  });
});